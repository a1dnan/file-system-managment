spring:
  datasource:
    url: jdbc:mysql://localhost:3306/file_system_db
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    database-platform: org.hibernate.dialect.MariaDBDialect
    generate-ddl: true
    show-sql: true
file:
  storage:
    location: STORAGE_FOLDER